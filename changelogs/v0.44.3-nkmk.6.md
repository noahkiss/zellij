# v0.44.3-nkmk.6

- **Upstream base:** `v0.44.3`
- **Range:** `9bf5f860d..4e4599cfd` (4 code commits, plus a style commit, 3 doc commits and the
  version bump)
- **Released:** 2026-08-05

*Back-written 2026-08-10 from the git history and FORK.md.*

Installing the service becomes a command of its own, and the config can shape the unit it writes.

- `zellij session enable|disable|status` installs, removes and inspects the service.
  See [`zellij session enable|disable|status`](../FORK.md#zellij-session-enabledisablestatus).
- `session_service` supplies extra directives for the generated unit.
  See [Extra unit directives from the config](../FORK.md#extra-unit-directives-from-the-config-session_service).
- Fix: the systemd unit no longer kills the session it started.
  See [`zellij setup --generate-service`](../FORK.md#zellij-setup---generate-service-systemdlaunchd).
- A client that reaches a server from a different build says so once, on stderr.
  See [A warning when the running session is a different build](../FORK.md#a-warning-when-the-running-session-is-a-different-build).
