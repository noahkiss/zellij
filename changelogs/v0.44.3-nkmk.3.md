# v0.44.3-nkmk.3

- **Upstream base:** `v0.44.3`
- **Range:** `a4d8f7456..5a1f7974b` (1 code commit, plus the version bump)
- **Released:** 2026-07-30

*Back-written 2026-08-10 from the git history and FORK.md.*

- Fix: `top_only` now wins over the stacked corner mapping, so a pane in a stack keeps top-only
  frames.
  Reshaped at 0.45.0: the standalone `top_only` option is gone, and the behaviour is now a
  fourth value of upstream's own `pane_frame_style`.
