# v0.44.3-nkmk.8

- **Upstream base:** `v0.44.3`
- **Range:** `3d7bd043a..cd0700db5` (2 code commits, plus the version bump)
- **Released:** 2026-08-05

*Back-written 2026-08-10 from the git history and FORK.md.*

- A temporary directory that cannot be created is an error, not a panic in `configure_logger`.
  See [A missing `TMPDIR` is an error, not a panic](../FORK.md#a-missing-tmpdir-is-an-error-not-a-panic).
- `session status` finds the timer through the service it starts, so a hand-written pair no longer
  reports the watchdog as missing.
  See [The timer is found through the service it starts](../FORK.md#the-timer-is-found-through-the-service-it-starts).
