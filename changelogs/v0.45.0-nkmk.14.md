# v0.45.0-nkmk.14

- **Upstream base:** `98a083707` on upstream `main` (workspace version `0.45.0`) — unchanged since
  nkmk.13
- **Range:** `v0.45.0-nkmk.13..main` (1 commit)
- **Released:** 2026-08-17

One fix, one line of code, and an honest caveat. Zellij stops emitting the byte that makes
mosh 1.4.0 drop the cell in the right-most column. That mosh mechanism is real and reproducible,
but zellij was never shown to trigger it — so this is hygiene, not the cure for a symptom you are
watching.

## Fixed

### OSC 8 hyperlinks end with BEL, not ST

`TERMINATOR` in `zellij-server/src/panes/link_handler.rs` is now BEL (`\x07`) instead of the 7-bit
string terminator (`ESC \`). Both the link start and the link end read that one constant, so both
change together.

The reason is mosh. `mosh-server` up to and including 1.4.0 does not parse OSC at all. Its terminal
emulator hands the whole sequence to an unknown-escape path, and that path clears the pending-wrap
flag as a side effect (mobile-shell/mosh#687). Pending-wrap is what remembers that the cursor has
written the last column and must wrap before the next character; clearing it discards the cell that
was waiting there.

Zellij emits an OSC 8 link *end* after every style reset, which in practice is at or near the start
of essentially every rendered row. So over mosh it emitted the trigger byte constantly. BEL takes a
different path in that emulator and leaves the flag alone. Fed a synthetic byte stream, mosh 1.4.0
loses the cell with ST and keeps it with BEL.

**What this does not fix.** The mechanism is real, but zellij's own render path was never shown to hit it. Zellij writes each
row as a chunk that starts with an absolute cursor move, and that move already clears mosh's
pending-wrap flag before any OSC 8 terminator in the chunk can. The change removes the trigger byte;
no observed symptom is known to depend on it.

So if you are seeing missing characters down the right edge of a pane over mosh, do not expect this
release to fix it. That evidence points at the terminal's font shaper, not at the terminator, and it
is still open.

Nothing is given up either. BEL is the older of the two OSC terminators and every terminal this fork
targets accepts it — zellij already ends its own window-title OSC with BEL. Links stay clickable in
Ghostty, iTerm2 and kitty, and `osc8_hyperlinks true`, which is the default, is unaffected.

The two unit tests in `link_handler.rs` build their expected strings from `TERMINATOR`, so they
follow the constant rather than pinning the old bytes. Proved end to end as well: a pty client
attached to a scratch session captured 174 OSC 8 sequences, all BEL-terminated and none
ST-terminated.

Deliberately unchanged: `zellij-utils/src/setup.rs` still writes ST-terminated links. That is the
`zellij setup --check` report, printed by the CLI to its own stdout rather than drawn through a
pane, so it is not on the path that loses the cell.

## Upstream

The base is unchanged at `98a083707`. No plugin API change and no contract change: a plugin built
against nkmk.6 or later keeps working, and the bundled plugins are unchanged.

## Upgrading

Nothing is required. No config key is added, removed or changed.

---

## Commits

```
dcebc7b93 fix(render): terminate OSC 8 hyperlinks with BEL, not ST
```
